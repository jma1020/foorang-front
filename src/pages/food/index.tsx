import type { NextPage } from "next";
import Navigator from "../../components/Navigator";

const Food: NextPage = () => {
  return (
    <div>
      <Navigator />
    </div>
  );
};

export default Food;
